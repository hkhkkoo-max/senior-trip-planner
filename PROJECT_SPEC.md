# 어르신 맞춤 당일치기 여행 도우미 — 개발 스펙

작성일: 2026-08-03
용도: 코딩 에이전트(Antigravity CLI 등)에 전달할 구현 스펙

---

## 1. 프로젝트 개요

성남시 분당구에 거주하는 70대 이상 어르신이 별도 로그인 없이 간단한 입력만으로 이동 2시간 이내 지역의 **당일치기** 여행 일정을 자동 생성받는 웹서비스.

- 초기 지역 범위: 분당구 기준 이동시간 2시간 이내 (지역 확장을 고려한 구조로 설계, 하드코딩 지양)
- 여행 유형: 당일치기 전용 (숙박 없음)
- 이동수단: 전기차 자가운전 기준 (주유비 계산 없음, 대신 충전소 정보 제공)
- 주 사용자: 70세 이상 어르신 본인이 직접 입력 폼 작성
- 결과 공유: 고유 링크 발급 → 카카오톡 등으로 가족/지인과 공유 (별도 계정 불필요)

## 2. 사용자 흐름

1. 서비스 접속 → 공용 비밀번호 입력 (일정 "생성" 시에만 필요)
2. 입력 폼 작성 (아래 5. 참고) — 한 화면에 한 질문씩, 단계별 진행
3. 서버가 LLM으로 일정 초안 생성
4. 지도/장소 API로 장소 실재 여부·위치·이동시간 검증, 필요 시 EV 충전소 위치 조회
5. 결과 화면 렌더링 + 고유 URL 발급 (DB에 결과 저장)
6. 사용자는 인쇄/PDF 저장 가능, 링크를 복사해 가족에게 공유
7. 링크로 결과를 열람하는 제3자는 비밀번호 없이 바로 조회 가능 (읽기 전용, 재생성 불가)

## 3. 접근 제어

- 회원가입/로그인 시스템 없음 (계정, 세션 개념 없음)
- **일정 생성 화면**에는 공용 비밀번호 게이트 적용 — 목적은 LLM/지도 API 비용(토큰) 남용 방지
  - 비밀번호는 환경변수로 관리 (`APP_PASSWORD` 등), 큰 글씨의 간단한 숫자 암호 권장
  - 비밀번호 검증 후 짧은 세션/쿠키로 재입력 방지 (선택)
- **결과 열람 페이지**(`/trip/{id}` 형태)는 비밀번호 없이 공개 — 이미 생성된 정적 결과만 보여주므로 API 비용 발생 없음

## 4. 입력 항목 (Input Spec)

| 필드명 | 타입 | 설명 |
|---|---|---|
| destination | string / "추천" | 여행지 직접 지정 또는 추천 요청 |
| lunchBudgetPerPerson | enum | `20000` / `30000` / `50000` (원, 1인당 점심 1회 기준) |
| interests | multi-select | 자연/둘레길, 사찰, 온천, 맛집, 전통시장, 문화유적 등 |
| companionCount | integer | 동행 인원 수 (관계 구분 없음, 본인 포함 총 인원) |
| transport | fixed value | "전기차 자가운전" 고정 (선택 UI 불필요, 명시만) |

※ 여행 기간·숙소 관련 입력 없음 (당일치기 고정이므로 폼에서 제외)
※ 출발 시각은 사용자 입력값이 아니라 시스템이 "오전 9시 이후"로 강제 생성

## 5. 출력 항목 (Output Spec)

| 필드명 | 설명 |
|---|---|
| overview | 전체 일정 요약 (당일 오전 9시 이후 출발 ~ 귀가) |
| timeline | 시간대별 세부 일정 (장소, 이동시간 포함) |
| estimatedCost | 점심 식비(1인당 예산 × 인원수) + 입장료 + 기타 비용, 항목별 합계. 숙박비·주유비 항목 없음 |
| routePlan | 이동 경로, 구간별 소요시간, 전기차 충전소 위치(좌표/이름) |
| checklist | 준비물 리스트 (계절/일정 기반) |
| caution | 건강·안전·날씨 관련 유의사항 |
| shareUrl | 결과 고유 링크 |

## 6. 핵심 비즈니스 규칙 / 제약사항

1. **실시간성 정보 금지**: 가격, 운영시간 등 자주 바뀌는 정보는 LLM이 임의로 사실처럼 생성하지 않고 `"확인 필요"`로 표시 + 가능하면 공식 링크/전화번호 병기. 프롬프트에 명시적으로 지시.
2. **장소 검증 필수**: LLM이 제안한 장소는 지도/장소 API로 실재 여부·좌표를 검증한 후에만 결과에 포함. 검증 실패 장소는 제외하거나 "확인 필요" 처리.
3. **당일치기 고정**: 숙박 관련 로직/필드 없음.
4. **전기차 기준**: 주유비 계산 없음. 대신 이동 경로 상 전기차 충전소 위치를 조회해 표시.
5. **출발 시각 고정**: 모든 일정은 09:00 이후 출발로 생성.
6. **식사는 점심만**: 아침/저녁 일정·비용 항목 없음.
7. **대중교통 정보 제외**: 이동 계획에 대중교통 경로/시간표 표시하지 않음.
8. **동행 인원**: 인원수만 반영, 관계(배우자/자녀/친구 등)는 프롬프트나 UI에서 묻지 않음.

## 7. 접근성 / UX 요구사항 (70대 이상 대상)

- 회원가입/로그인 없이 즉시 이용
- 한 화면 한 질문 방식의 단계별(마법사) 입력 UI
- 기본 폰트 크기 18px 이상, 고대비 배색, 큰 버튼(터치 타깃 44px 이상 권장)
- 쉬운 용어 사용, 아이콘 + 텍스트 병행 표기
- 결과 화면은 인쇄/PDF 저장 가능한 레이아웃
- (2차 고려) 음성 입력, 보호자 대리 입력 모드

## 8. 기술 스택

| 영역 | 선택 | 비고 |
|---|---|---|
| 프론트엔드 | Next.js (반응형, 접근성 우선) | |
| 일정 생성 | LLM API (Claude 등) | 실시간 정보 생성 금지 지시를 시스템 프롬프트에 고정 포함 |
| 장소 검증/지도 | 카카오맵 API (REST) | 국내 장소 데이터 풍부, 월 300만 건 무료 쿼터로 충분 |
| EV 충전소 정보 | 공공데이터포털 전기차 충전소 API | |
| 데이터 저장 | 경량 DB (예: SQLite/서버리스 KV) | 로그인 없이 결과를 ID 기반으로 저장 |
| 접근 제어 | 환경변수 기반 공용 비밀번호 게이트 | 계정 시스템 없이 미들웨어 수준에서 구현 |
| 배포 | 서버리스 호스팅 | |

## 9. 데이터 모델 (초안)

```
Trip {
  id: string (UUID, 공유 링크에 사용)
  createdAt: datetime
  input: {
    destination: string
    lunchBudgetPerPerson: number
    interests: string[]
    companionCount: number
  }
  output: {
    overview: string
    timeline: TimelineItem[]
    estimatedCost: CostBreakdown
    routePlan: RouteInfo
    checklist: string[]
    caution: string[]
  }
}
```

## 10. API 비용 참고 (2026-08 기준)

- 예상 사용량: 월 10회 이내 (일정 생성 기준)
- 카카오맵 API: 무료 쿼터 월 300만 건(전체 API), 장소검색/경로조회 등 일 1,000~30만 건 → 월 10회는 예산상 사실상 0원. 단, **개발자 계정 기준 "첫 번째로 활성화한 앱"에만 무료 쿼터 적용**되므로 신규 계정에서 처음 만드는 앱으로 발급할 것
- 네이버 지도 API 대안 사용 시: API별 월 최소 3,000건~최대 1억 건 무료,단 "대표계정(가장 먼저 사용한 계정)"에만 적용
- LLM API 비용은 별도 확인 필요 (모델/토큰 단가에 따라 상이)

## 11. MVP 범위

**포함**
- 공용 비밀번호 게이트 → 입력 폼 → LLM 일정 생성 → 지도 API 장소 검증 → 결과 화면 + 공유 링크
- 분당구 중심 이동 2시간 이내 지역만 지원
- 당일치기, 점심 1회, 전기차 기준 로직 전부 반영

**제외 (추후 확장)**
- 회원 관리 기능
- 전국 지역 확장
- 실시간 가격/예약 연동
- 다국어 지원
- 음성 입력, 보호자 대리 입력 모드

## 12. 아직 미결정 사항

- 서비스명 및 브랜딩
- LLM API 비용 부담 방식/모델 선택
- 공용 비밀번호 값 및 배포 환경(도메인, 호스팅 계정)
